apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "theoros.fullname" . }}-db-pvc
  labels:
    {{- include "theoros.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi