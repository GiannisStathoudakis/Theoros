apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cluster-addons
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - app: vault
            path: Apps/Vault
          - app: cilium
            path: Apps/Cilium
          - app: eso
            path: Apps/External-Secrets
          - app: cert-manager
            path: Apps/Cert-Manager
          - app: envoy
            path: Apps/Envoy
          - app: argocd
            path: Apps/ArgoCD

  template:
    metadata:
      name: '{{app}}-root'
    spec:
      project: default
      source:
        repoURL: git@github.com:GiannisStathoudakis/Theoros.git
        targetRevision: HEAD
        path: 'Testing_Infra/{{path}}'
      destination:
        server: https://kubernetes.default.svc
      syncPolicy:
        automated:
          prune: true
          selfHeal: true